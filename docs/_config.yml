title: mcptools
description: "mitmproxy for MCP — intercept, inspect, debug, and replay MCP server traffic"
url: "https://jannik-cas.github.io"
baseurl: "/mcptools"
remote_theme: pages-themes/hacker@v0.2.0
plugins:
  - jekyll-remote-theme

markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge

defaults:
  - scope:
      path: ""
    values:
      layout: "default"
